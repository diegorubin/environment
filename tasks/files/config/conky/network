conky.config = {
    color1 = '#d79921',
    alignment = 'top_left',
    background = false,
    border_width = 30,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'Fantasque Sans Mono:size=12',
    gap_x = 130,
    gap_y = 480,
    minimum_height = 5,
    minimum_width = 330,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 190,
    own_window_hints = 'below',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 2.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${color1}${font Fantasque Sans Mono:size=16}Networking
${color}${font}wlp60s0

${color1}${font}Download:${color}   ${downspeed wlp60s0}
${color1}${font}Total:${color}      ${totaldown wlp60s0}
${color1}${color1}${downspeedgraph wlp60s0 50,330 444444 eeeeee}
 
${color1}${font}Upload:${color}     ${upspeed wlp60s0} 
${color1}${font}Total${color}       ${totalup wlp60s0} 
${color1}${upspeedgraph wlp60s0 50,330 444444 eeeeee}
]]
